---
phase: 05-base-image-and-skill-schema
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - libs/kubex-common/kubex_common/schemas/config.py
  - services/kubex-manager/kubex_manager/skill_validator.py
  - services/kubex-manager/kubex_manager/skill_resolver.py
  - services/kubex-manager/kubex_manager/blocklist.yaml
  - services/kubex-manager/kubex_manager/__init__.py
  - services/kubex-manager/kubex_manager/lifecycle.py
  - agents/_base/kubex_harness/config_loader.py
  - agents/_base/kubex_harness/skill_loader.py
  - agents/_base/kubex_harness/main.py
  - agents/_base/kubex_harness/standalone.py
  - agents/_base/entrypoint.sh
  - agents/_base/Dockerfile
  - skills/data-collection/web-scraping/skill.yaml
  - skills/knowledge/recall/skill.yaml
  - skills/dispatch/task-management/skill.yaml
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
  - BASE-04
  - SKIL-01
  - SKIL-02
  - SKIL-03
  - SKIL-04
must_haves:
  truths:
    - "SkillManifest Pydantic model validates the finalized minimal schema (no policy/budget/actions)"
    - "SkillValidator detects injection patterns via regex and returns structured verdict"
    - "SkillResolver composes multiple skills into a single ComposedSkillSet with namespaced tools"
    - "config_loader reads config.yaml and falls back to env vars"
    - "entrypoint.sh installs pip/system deps from env vars before starting harness"
    - "create_kubex() assembles Docker bind mounts for skill directories at spawn"
    - "All red tests from 05-01 now pass (green)"
  artifacts:
    - path: "libs/kubex-common/kubex_common/schemas/config.py"
      provides: "Rewritten SkillManifest, SkillDependencies, SkillTool, ValidationStamp models"
      contains: "class SkillManifest"
    - path: "services/kubex-manager/kubex_manager/skill_validator.py"
      provides: "SkillValidator with regex + LM dual-layer, stamp logic, CLI entry point"
      exports: ["SkillValidator", "ValidationVerdict", "ValidationStamp"]
    - path: "services/kubex-manager/kubex_manager/skill_resolver.py"
      provides: "SkillResolver with composition, tool namespacing, version conflict detection"
      exports: ["SkillResolver", "ComposedSkillSet", "SkillResolutionError"]
    - path: "services/kubex-manager/kubex_manager/blocklist.yaml"
      provides: "Seed regex injection blocklist patterns"
      contains: "ignore.*instructions"
    - path: "services/kubex-manager/kubex_manager/lifecycle.py"
      provides: "Extended create_kubex() with skill bind-mount assembly"
      contains: "skill_mounts"
    - path: "agents/_base/kubex_harness/config_loader.py"
      provides: "load_agent_config() — reads config.yaml, falls back to env vars"
      exports: ["load_agent_config"]
    - path: "agents/_base/kubex_harness/skill_loader.py"
      provides: "Skill loading with composition support, wraps _load_skill_files"
      exports: ["load_skills_from_config"]
    - path: "agents/_base/kubex_harness/main.py"
      provides: "Unified entry point — routes to standalone or openclaw based on config"
      contains: "harness_mode"
    - path: "agents/_base/entrypoint.sh"
      provides: "Dep install step before harness start"
      contains: "KUBEX_PIP_DEPS"
  key_links:
    - from: "services/kubex-manager/kubex_manager/skill_validator.py"
      to: "services/kubex-manager/kubex_manager/blocklist.yaml"
      via: "YAML load at init"
      pattern: "yaml\\.safe_load.*blocklist"
    - from: "services/kubex-manager/kubex_manager/skill_resolver.py"
      to: "libs/kubex-common/kubex_common/schemas/config.py"
      via: "imports SkillManifest, SkillTool, SkillDependencies"
      pattern: "from kubex_common\\.schemas\\.config import"
    - from: "agents/_base/kubex_harness/config_loader.py"
      to: "libs/kubex-common/kubex_common/schemas/config.py"
      via: "imports AgentConfig"
      pattern: "from kubex_common\\.schemas\\.config import"
    - from: "agents/_base/kubex_harness/main.py"
      to: "agents/_base/kubex_harness/config_loader.py"
      via: "calls load_agent_config()"
      pattern: "load_agent_config"
    - from: "agents/_base/entrypoint.sh"
      to: "KUBEX_PIP_DEPS env var"
      via: "reads env var set by Manager"
      pattern: "KUBEX_PIP_DEPS"
    - from: "services/kubex-manager/kubex_manager/lifecycle.py"
      to: "skill directories on host"
      via: "Docker SDK volumes dict for bind mounts"
      pattern: "volumes.*skill"
---

<objective>
Implement all Phase 5 production code to make the red tests from 05-01 pass: rewrite SkillManifest schema, create SkillValidator (regex + LM injection defense), create SkillResolver (skill composition with tool namespacing), create config_loader (config.yaml + env var fallback), unify the harness entry point, extend entrypoint.sh with dep install, extend create_kubex() with skill bind mounts (SKIL-02), and migrate existing skill.yaml files to the new schema.

Purpose: Deliver the "stem cell" base image contract — any Kubex can become any agent via config and skills.

Output: All 05-01 tests go green. Production modules ready for Phase 6 (Manager spawn logic) to consume.

**Phase 6 handoffs (explicitly deferred from Phase 5):**
- **LM eval result caching**: CONTEXT.md specifies "LM eval results cached with hash key (hash of skill manifests + policy config)." This requires the Manager spawn pipeline (Phase 6 KMGR scope) to compute the composite hash across skill manifests AND policy config. Phase 5 delivers the per-skill `ValidationStamp` with content hash; Phase 6 composes these into a cache key when the Manager assembles spawn parameters.
- **ESCALATE routing for injection detection**: CONTEXT.md specifies "When injection detected, ESCALATE to human (not hard reject)." Phase 5 delivers `ValidationVerdict(is_clean=False)` from `SkillValidator`. Phase 6 wires this to the Gateway's existing ESCALATE endpoint (PSEC-01/PSEC-02) as part of the policy gate pipeline.
- **Auto-add patterns to blocklist + re-scan**: CONTEXT.md specifies "LM auto-adds patterns to blocklist, triggers re-scan." Phase 5 delivers the blocklist.yaml structure and `validate_catalog()` API. Phase 6 implements the write-back flow (LM writes `blocklist.pending.yaml`, human reviews, merged entries trigger catalog re-scan via `validate_catalog()`).
</objective>

<execution_context>
@C:/Users/naaht/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/naaht/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-base-image-and-skill-schema/05-CONTEXT.md
@.planning/phases/05-base-image-and-skill-schema/05-RESEARCH.md
@.planning/phases/05-base-image-and-skill-schema/05-01-SUMMARY.md

@libs/kubex-common/kubex_common/schemas/config.py
@agents/_base/kubex_harness/standalone.py
@agents/_base/kubex_harness/main.py
@agents/_base/kubex_harness/harness.py
@agents/_base/entrypoint.sh
@agents/_base/Dockerfile
@services/kubex-manager/kubex_manager/lifecycle.py
@services/kubex-manager/kubex_manager/__init__.py
@skills/data-collection/web-scraping/skill.yaml
@tests/unit/test_skill_validator.py
@tests/unit/test_skill_resolver.py
@tests/unit/test_config_loader.py
@tests/unit/test_kubex_manager_unit.py

<interfaces>
<!-- These are the test expectations from 05-01 that this plan must satisfy -->

Expected SkillManifest (rewrite in config.py):
```python
class SkillDependencies(BaseModel):
    pip: list[str] = Field(default_factory=list)
    system: list[str] = Field(default_factory=list)

class SkillTool(BaseModel):
    name: str
    description: str = ""
    parameters: dict[str, Any] = Field(default_factory=dict)

class ValidationStamp(BaseModel):
    content_hash: str
    validated_at: str
    validator_version: str
    verdict: str

class SkillManifest(BaseModel):
    model_config = ConfigDict(extra="forbid")  # Rejects old fields
    name: str
    version: str = "0.1.0"
    description: str = ""
    category: str = ""
    capabilities: list[str] = Field(default_factory=list)
    tools: list[SkillTool] = Field(default_factory=list)
    dependencies: SkillDependencies = Field(default_factory=SkillDependencies)
    egress_domains: list[str] = Field(default_factory=list)
    validation_stamp: ValidationStamp | None = None
```

Expected SkillValidator API:
```python
class SkillValidator:
    def __init__(self, blocklist_path: Path, lm_client: LMClient | None = None): ...
    def validate_skill_md(self, skill_name: str, content: str) -> ValidationVerdict: ...
    def validate_catalog(self, skills_dir: Path) -> list[ValidationVerdict]: ...
# CLI: python -m kubex_manager.skill_validator skills/
```

Expected SkillResolver API:
```python
class SkillResolver:
    def resolve(self, skill_names: list[str], skill_dir: Path) -> ComposedSkillSet: ...
# ComposedSkillSet: capabilities, pip_deps, system_deps, egress_domains, tools (namespaced), ordered_skill_names, version_conflicts
```

Expected config_loader API:
```python
def load_agent_config(config_path: str = "/app/config.yaml") -> AgentConfig: ...
# Reads config.yaml, falls back to env vars, supports harness_mode field
```

From lifecycle.py (existing Docker SDK pattern):
```python
class KubexLifecycle:
    # create_kubex() uses docker.containers.create() with volumes dict
    # Pattern: {host_path: {"bind": container_path, "mode": "ro"}}
    # Currently handles credential mounts only
    # SKIL-02: extend to also mount skill directories
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SkillManifest schema, create SkillValidator + SkillResolver, extend create_kubex() for skill bind mounts</name>
  <files>libs/kubex-common/kubex_common/schemas/config.py, services/kubex-manager/kubex_manager/skill_validator.py, services/kubex-manager/kubex_manager/skill_resolver.py, services/kubex-manager/kubex_manager/blocklist.yaml, services/kubex-manager/kubex_manager/__init__.py, services/kubex-manager/kubex_manager/lifecycle.py, skills/data-collection/web-scraping/skill.yaml, skills/knowledge/recall/skill.yaml, skills/dispatch/task-management/skill.yaml</files>
  <action>
**1. Rewrite SkillManifest in config.py (SKIL-01):**
- Add `SkillDependencies`, `SkillTool`, `ValidationStamp` models per the interface spec above.
- Rewrite `SkillManifest` with `model_config = ConfigDict(extra="forbid")` to reject old fields (`actions_required`, `resource_requirements`, `system_prompt_section`).
- Remove old fields. Keep `name`, `version`, `description`, `category`, `capabilities`, `egress_domains`. Change `tools` from `list[str]` to `list[SkillTool]`. Add `dependencies: SkillDependencies`, `validation_stamp: ValidationStamp | None`.
- Add `harness_mode: str = "standalone"` field to `AgentConfig` for routing (BASE-04).
- CRITICAL: Grep all imports of `SkillManifest`, `actions_required`, `resource_requirements`, `system_prompt_section` across the codebase and update every call site. Check `lifecycle.py`, `test_kubex_manager_unit.py`, `test_harness_unit.py`, any agent config files. This is a breaking change — update ALL usages atomically.

**2. Create SkillValidator (SKIL-04):**
- Create `services/kubex-manager/kubex_manager/skill_validator.py` with:
  - `LMVerdict` model: `is_clean: bool`, `issues: list[str]`, `analysis: str = ""`
  - `ValidationVerdict` model: `is_clean: bool`, `detected_patterns: list[str]`, `lm_analysis: LMVerdict | None`, `stamp: ValidationStamp | None`
  - `SkillValidator` class:
    - `__init__(self, blocklist_path: Path, lm_client: Any | None = None)` — loads blocklist YAML
    - `_regex_check(self, content: str) -> list[str]` — compiles patterns from blocklist, returns list of matched pattern IDs
    - `_lm_check(self, content: str) -> LMVerdict` — calls LM client (when not None) with structured analysis prompt
    - `_create_stamp(self, content: str) -> ValidationStamp` — sha256 hash, ISO timestamp, validator version "1.0.0", verdict "clean"
    - `validate_skill_md(self, skill_name: str, content: str) -> ValidationVerdict` — regex first, then LM if regex passes and client exists, stamp if clean
    - `validate_catalog(self, skills_dir: Path) -> list[ValidationVerdict]` — walks skill dirs, reads SKILL.md from each, validates
  - `__main__` block for CLI: `python -m kubex_manager.skill_validator skills/` — prints results, exits 0 if all clean, 1 if any dirty
- Create `services/kubex-manager/kubex_manager/blocklist.yaml` with the 5 seed patterns from the RESEARCH.md (ignore-previous-instructions, disregard-system-prompt, you-are-now, jailbreak-do-anything, exfiltrate-data).

**3. Create SkillResolver (SKIL-03):**
- Create `services/kubex-manager/kubex_manager/skill_resolver.py` with:
  - `SkillResolutionError(Exception)` for version conflicts
  - `ComposedSkillSet` dataclass with: `capabilities`, `pip_deps`, `system_deps`, `egress_domains` (all `list[str]`, deduplicated), `tools` (`dict[str, SkillTool]`, namespaced as `"skill-dir-name.tool-name"`), `ordered_skill_names` (preserves config order), `version_conflicts` (`list[str]`)
  - `SkillResolver` class:
    - `resolve(self, skill_names: list[str], skill_dir: Path) -> ComposedSkillSet`
    - Reads `skill.yaml` from each `skill_dir/{category}/{name}/skill.yaml` (finding by name across categories)
    - Union all capabilities (deduplicated), union all deps (check for version pin conflicts), namespace tools as `"dir-name.tool-name"`
    - Raise `SkillResolutionError` if version conflicts exist (e.g., `requests==2.31` vs `requests==2.28`)
    - Preserve skill ordering from input list

**4. Extend create_kubex() for skill bind mounts (SKIL-02):**
- In `services/kubex-manager/kubex_manager/lifecycle.py`, extend `create_kubex()` to accept skill mount information.
- Add a `skill_mounts` field to `CreateKubexRequest` (or extract from the request's skill config) — a list of skill directory names to bind-mount.
- After the existing credential volume assembly block, add skill volume assembly:
  - For each skill name in `skill_mounts`, compute the host path (e.g., `os.environ.get("KUBEX_SKILLS_PATH", "/app/skills")/{skill_name}`) and container path (`/app/skills/{skill_name}`).
  - Add to the `volumes` dict: `{host_path: {"bind": f"/app/skills/{skill_name}", "mode": "ro"}}`.
  - Skills are read-only mounts — agents must not modify skill files.
- This makes the `test_bind_mounts_skills` xfail test from 05-01 pass.

**5. Migrate existing skill.yaml files:**
- Update `skills/data-collection/web-scraping/skill.yaml`: Remove `policy`, `budget`, `actions` fields. Add `dependencies: {pip: [], system: []}` and `capabilities` list. Ensure `tools` uses `SkillTool` format (name + description).
- Update `skills/knowledge/recall/skill.yaml` and `skills/dispatch/task-management/skill.yaml` similarly.
- Ensure all 3 skill.yaml files validate against the new `SkillManifest` schema.

**6. Update `__init__.py`:**
- Add imports for `skill_validator` and `skill_resolver` modules in the kubex-manager package init if needed for discovery.
  </action>
  <verify>
    <automated>cd D:/dev/dev/openclaw && python -m pytest tests/unit/test_skill_validator.py tests/unit/test_skill_resolver.py tests/unit/test_kubex_manager_unit.py::test_bind_mounts_skills -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>SkillManifest rewritten with extra="forbid". SkillValidator detects all 5 seed injection patterns and stamps clean skills. SkillResolver composes skills with namespaced tools and detects version conflicts. create_kubex() assembles skill bind mounts in Docker volumes dict. All unit tests for SKIL-01, SKIL-02, SKIL-03, SKIL-04 pass. Existing tests updated for schema change.</done>
</task>

<task type="auto">
  <name>Task 2: Create config_loader, unify harness entry point, extend entrypoint.sh, update Dockerfile</name>
  <files>agents/_base/kubex_harness/config_loader.py, agents/_base/kubex_harness/skill_loader.py, agents/_base/kubex_harness/main.py, agents/_base/kubex_harness/standalone.py, agents/_base/entrypoint.sh, agents/_base/Dockerfile</files>
  <action>
**1. Create config_loader.py (BASE-02, BASE-04):**
- Create `agents/_base/kubex_harness/config_loader.py` with:
  - `load_agent_config(config_path: str = "/app/config.yaml") -> AgentConfig`
  - Try reading `config_path` with pyyaml. Parse the `agent:` stanza into `AgentConfig`.
  - If file not found, fall back to env vars (backward compat with current `StandaloneConfig` behavior). Read `KUBEX_MODEL`, `KUBEX_CAPABILITIES`, `KUBEX_AGENT_ID`, etc.
  - Env vars OVERRIDE config.yaml values when both present (per user decision).
  - Support `harness_mode` field: `"standalone"` (default) or `"openclaw"`. Determines which loop runs.
  - Parse `skills` list from config (skill directory names for SkillResolver to resolve later).

**2. Create skill_loader.py:**
- Create `agents/_base/kubex_harness/skill_loader.py` wrapping the existing `_load_skill_files()` function.
- `load_skills_from_config(skills_dir: str = "/app/skills", skill_order: list[str] | None = None) -> str`
  - If `skill_order` provided, load skills in that order (from config.yaml). Skills listed first get priority in prompt.
  - Each skill gets its own labeled section (preserve existing `--- Skill: {name} ---` format).
  - Return the composed prompt string.
- Keep `_load_skill_files` accessible at its current import path in `standalone.py` (re-export) to avoid breaking existing tests.

**3. Rewrite main.py (BASE-04):**
- Rewrite `agents/_base/kubex_harness/main.py` as the unified entry point.
- On boot: call `load_agent_config()` to get config.
- If `harness_mode == "openclaw"`: import and run `KubexHarness` from `harness.py`.
- If `harness_mode == "standalone"` (default): import and run `StandaloneAgent` from `standalone.py`.
- Log structured boot summary: loaded skills, active tools, model, capabilities (per user decision).
- Keep backward compat: if no config.yaml and no env vars, log warning and exit.

**4. Extend entrypoint.sh (BASE-03):**
- Add dependency install step BEFORE the harness start:
  - Read `KUBEX_PIP_DEPS` env var (space-separated pip packages). If non-empty: `pip install --no-cache-dir --quiet $KUBEX_PIP_DEPS || { echo "[entrypoint] FATAL: pip install failed"; exit 1; }`
  - Read `KUBEX_SYSTEM_DEPS` env var (space-separated apt packages). If non-empty: `apt-get update -qq && apt-get install -y --no-install-recommends $KUBEX_SYSTEM_DEPS || { echo "[entrypoint] FATAL: apt install failed"; exit 1; }`
  - Per user decision: fail fast on dep install failure — container exits with clear error.
- Log boot summary after dep install: "Dependencies installed: pip=[...], system=[...]"
- Config mount path: `/app/config.yaml` (Claude's discretion, consistent with `/app/skills`).

**5. Update Dockerfile (BASE-01):**
- Ensure `CMD` points to unified `main.py` entry point (not standalone.py).
- Ensure `/app/config.yaml` mount point is documented in a LABEL or comment.
- Ensure `/app/skills` volume mount point preserved.
- No per-agent Dockerfiles — this IS the universal base image.

**6. Preserve backward compatibility:**
- `standalone.py`: Keep `_load_skill_files` function accessible (re-export from skill_loader or keep in place). Keep `StandaloneConfig` class (used as fallback). Keep `StandaloneAgent` class (called by main.py).
- Do NOT break imports that existing tests rely on: `from kubex_harness.standalone import _load_skill_files, StandaloneConfig`.
  </action>
  <verify>
    <automated>cd D:/dev/dev/openclaw && python -m pytest tests/unit/test_config_loader.py tests/unit/test_harness_unit.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>config_loader loads config.yaml with env var fallback. main.py routes to standalone/openclaw based on harness_mode. entrypoint.sh installs deps from KUBEX_PIP_DEPS/KUBEX_SYSTEM_DEPS. Dockerfile CMD points to unified main.py. All config_loader tests pass. Existing harness unit tests (TestSkillInjection) still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite, fix any regressions, and commit green implementation</name>
  <files></files>
  <action>
1. Run: `python -m pytest tests/unit/ -x -v` — all unit tests must pass (new AND existing).
2. Run: `python -m pytest tests/ libs/ services/ -x -q` — full suite (excluding Docker E2E that needs daemon).
3. If any existing tests fail due to SkillManifest schema change:
   - Grep for the failing import/attribute and fix.
   - Common failure: `SkillManifest(actions_required=...)` in test fixtures — update to new schema.
   - Common failure: `from kubex_harness.standalone import _load_skill_files` — ensure re-export works.
4. Verify the skill validator CLI works: `python -m kubex_manager.skill_validator skills/` — must exit 0 on the shipped (now-migrated) skill files.
5. Verify skill bind mount test passes: `python -m pytest tests/unit/test_kubex_manager_unit.py::test_bind_mounts_skills -v` — must PASS (not xfail).
6. Count total tests: should be 827+ existing + new tests from 05-01.
7. Commit with message: `Phase 5: base image, skill schema, validator, resolver, config loader (green)\n\n- SkillManifest rewritten (extra=forbid, removed policy/budget/actions)\n- SkillValidator: regex + LM dual-layer injection defense with stamps\n- SkillResolver: skill composition, tool namespacing, version conflict detection\n- config_loader: config.yaml primary, env var fallback, harness_mode routing\n- create_kubex(): skill bind mounts via Docker SDK volumes (SKIL-02)\n- entrypoint.sh: dep install from KUBEX_PIP_DEPS/KUBEX_SYSTEM_DEPS\n- Unified main.py entry point\n- 3 skill.yaml files migrated to new schema\n- N tests passing (M new + 827 existing)\n- Phase 6 handoffs: LM eval cache, ESCALATE wiring, blocklist auto-add`
  </action>
  <verify>
    <automated>cd D:/dev/dev/openclaw && python -m pytest tests/ libs/ services/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite passes (827+ existing + all new tests). No regressions. Green implementation committed per implement-feature workflow.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/test_skill_validator.py tests/unit/test_skill_resolver.py tests/unit/test_config_loader.py tests/unit/test_kubex_manager_unit.py -v` — all PASS (not skip)
- `python -m pytest tests/unit/test_harness_unit.py::TestSkillInjection -v` — existing 9 tests still PASS
- `python -m pytest tests/ libs/ services/ -x -q` — full suite green
- `python -m kubex_manager.skill_validator skills/` — exits 0
- `python -c "from kubex_common.schemas.config import SkillManifest, SkillTool, SkillDependencies, ValidationStamp"` — imports succeed
- `python -c "from kubex_manager.skill_validator import SkillValidator, ValidationVerdict"` — imports succeed
- `python -c "from kubex_manager.skill_resolver import SkillResolver, ComposedSkillSet"` — imports succeed
- `python -c "from kubex_harness.config_loader import load_agent_config"` — imports succeed
</verification>

<success_criteria>
- All 05-01 red tests now pass (green), including test_bind_mounts_skills
- SkillManifest rejects old fields (extra="forbid")
- SkillValidator detects injection patterns and stamps clean skills
- SkillResolver composes skills with namespaced tools
- create_kubex() assembles skill directory bind mounts in Docker SDK volumes dict (SKIL-02)
- config_loader reads config.yaml with env var fallback
- entrypoint.sh installs deps before harness start
- main.py routes to standalone/openclaw based on config
- 3 existing skill.yaml files migrated to new schema
- All 827+ existing tests still pass (no regressions)
- Green implementation committed
- Phase 6 handoffs documented: LM eval cache, ESCALATE wiring, blocklist auto-add
</success_criteria>

<output>
After completion, create `.planning/phases/05-base-image-and-skill-schema/05-02-SUMMARY.md`
</output>
