---
phase: 08.1-agent-system-prompts
plan: 01
subsystem: agent-harness
tags: [pydantic, system-prompt, prompt-builder, policy, budget, kubex-harness]

requires:
  - phase: 08-mcp-bridge
    provides: AgentConfig with description/boundary fields, standalone harness, config_loader

provides:
  - PolicyConfig and BudgetConfig Pydantic sub-models parsed from config.yaml
  - AgentConfig extended with policy and budget fields (all existing agents updated)
  - PREAMBLE.md template with 8 placeholder variables and 5 required sections
  - prompt_builder.py module with build_system_prompt() that fills template and appends skill content

affects:
  - 08.1-02 (wires prompt_builder into standalone.py and mcp_bridge.py)
  - 08.2-vault-persistence (knowledge agent system prompt shapes vault operation behavior)

tech-stack:
  added: []
  patterns:
    - "PREAMBLE.md template + str.format_map() for structured system prompt assembly"
    - "PolicyConfig/BudgetConfig as typed sub-models nested inside AgentConfig"
    - "prompt_builder module resolves PREAMBLE.md via Path(__file__).parent for cwd independence"

key-files:
  created:
    - agents/_base/kubex_harness/PREAMBLE.md
    - agents/_base/kubex_harness/prompt_builder.py
  modified:
    - agents/_base/kubex_harness/config_loader.py

key-decisions:
  - "Double-brace escaping ({{ }}) used in PREAMBLE.md JSON output contract examples so str.format_map() does not interpret JSON braces as template variables"
  - "worker_list_section placeholder renders as empty string for worker agents (None passed); filled only by orchestrator path"
  - "Policy and budget parse defensively: missing stanza in config.yaml (hello-world) produces safe defaults, not an error"

patterns-established:
  - "Preamble-first prompt: structured identity + boundaries + security directives always prepend before skill content"
  - "Graceful fallback: if PREAMBLE.md is absent at runtime, build_system_prompt returns skill content or minimal default string"

requirements-completed: [PROMPT-01, PROMPT-02, PROMPT-03, PROMPT-04]

duration: 2min
completed: 2026-03-22
---

# Phase 08.1 Plan 01: Agent System Prompts Foundation Summary

**PREAMBLE.md template + prompt_builder.py module that assembles structured system prompts from AgentConfig policy/budget fields, capability boundaries, security directives, and skill content**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-22T05:17:11Z
- **Completed:** 2026-03-22T05:19:56Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Extended AgentConfig with PolicyConfig (allowed_actions, blocked_actions) and BudgetConfig (per_task_token_limit, daily_cost_limit_usd) sub-models, parsed from config.yaml policy/budget stanzas
- Created PREAMBLE.md template with 8 placeholder variables covering agent identity, system context, capability boundaries, policy summary, budget awareness, security directives, and output contract
- Created prompt_builder.py with build_system_prompt() that fills the template and appends skill content; handles orchestrator (worker list) vs worker (empty section) distinctions
- All 456 unit tests pass with zero regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend AgentConfig with policy/budget fields + create PREAMBLE.md** - `72b81d1` (feat)
2. **Task 2: Create prompt_builder.py module** - `55699c4` (feat)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified

- `agents/_base/kubex_harness/config_loader.py` - Added PolicyConfig, BudgetConfig models; AgentConfig extended with policy and budget fields; load_agent_config() parses both stanzas
- `agents/_base/kubex_harness/PREAMBLE.md` - System prompt template with 8 placeholders: {agent_id}, {description}, {capabilities}, {boundary}, {policy_summary}, {budget_summary}, {worker_list_section}, plus JSON output contract with escaped braces
- `agents/_base/kubex_harness/prompt_builder.py` - build_system_prompt(), _format_policy_summary(), _format_budget_summary(), _format_worker_list(); resolves PREAMBLE.md via Path(__file__).parent

## Decisions Made

- Double-brace escaping in PREAMBLE.md JSON examples (`{{` / `}}`) so str.format_map() does not misinterpret JSON curly braces as template variables
- worker_list_section renders as empty string for worker agents; only filled when orchestrator passes worker_descriptions list
- policy and budget parse defensively: configs with no policy/budget stanza (hello-world) get safe empty defaults

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- prompt_builder.py is ready to be wired into standalone.py and mcp_bridge.py (Plan 08.1-02)
- All existing agents (orchestrator, knowledge, reviewer, instagram-scraper) have policy/budget stanzas in their config.yaml files — will be picked up automatically at next boot
- hello-world agent has no policy/budget stanzas — safe defaults work correctly

---
*Phase: 08.1-agent-system-prompts*
*Completed: 2026-03-22*

## Self-Check: PASSED

- FOUND: agents/_base/kubex_harness/PREAMBLE.md
- FOUND: agents/_base/kubex_harness/prompt_builder.py
- FOUND: agents/_base/kubex_harness/config_loader.py
- FOUND: .planning/phases/08.1-agent-system-prompts/08.1-01-SUMMARY.md
- FOUND: commit 72b81d1 (Task 1)
- FOUND: commit 55699c4 (Task 2)
