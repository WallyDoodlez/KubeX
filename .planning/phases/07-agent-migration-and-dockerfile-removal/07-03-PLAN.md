---
phase: 07-agent-migration-and-dockerfile-removal
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - tests/conftest.py
  - tests/unit/test_config_loader.py
  - tests/unit/test_harness_unit.py
  - tests/unit/test_orchestrator_loop.py
  - tests/e2e/test_agent_harness.py
  - tests/e2e/test_agent_migration.py
  - tests/unit/test_no_agent_dockerfiles.py
  - tests/e2e/test_hello_world_spawn.py
autonomous: true
requirements: [MIGR-05]

must_haves:
  truths:
    - "Full test suite passes with 856+ tests and 0 failures"
    - "No test imports StandaloneConfig"
    - "No test sets KUBEX_AGENT_ID env var for config initialization"
    - "A session-scoped conftest fixture provides default config.yaml for all tests"
    - "All xfail markers from Plan 01 tests are removed (tests pass green)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Session-scoped default_agent_config fixture"
      contains: "default_agent_config"
    - path: "tests/unit/test_config_loader.py"
      provides: "Updated config loader tests for fail-fast behavior"
      contains: "test_missing_config_raises"
  key_links:
    - from: "tests/conftest.py"
      to: "agents/_base/kubex_harness/config_loader.py"
      via: "fixture writes config.yaml and patches config path"
      pattern: "load_agent_config"
    - from: "tests/unit/test_orchestrator_loop.py"
      to: "skills/orchestration/task-management/tools/"
      via: "updated imports after orchestrator_loop.py deletion"
      pattern: "import.*tools"
---

<objective>
Migrate the test suite to work without StandaloneConfig: add a conftest fixture for default config.yaml, update all tests that reference StandaloneConfig or KUBEX_AGENT_ID env vars, remove xfail markers from Plan 01 tests, and verify the full 856+ test suite passes green.

Purpose: Complete MIGR-05 (all tests pass against refactored agents). This is the dedicated test migration plan per user decision.
Output: Updated test files, conftest fixture, full green test suite.
</objective>

<execution_context>
@C:/Users/naaht/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/naaht/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agent-migration-and-dockerfile-removal/07-CONTEXT.md
@.planning/phases/07-agent-migration-and-dockerfile-removal/07-RESEARCH.md
@.planning/phases/07-agent-migration-and-dockerfile-removal/07-01-SUMMARY.md
@.planning/phases/07-agent-migration-and-dockerfile-removal/07-02-SUMMARY.md

<interfaces>
<!-- After Plan 02, these are the new interfaces -->

From agents/_base/kubex_harness/config_loader.py (after Plan 02):
```python
class AgentConfig(BaseModel):
    agent_id: str = ""
    model: str = "gpt-5.2"
    skills: list[str] = Field(default_factory=list)
    capabilities: list[str] = Field(default_factory=list)
    harness_mode: str = "standalone"
    gateway_url: str = "http://gateway:8080"
    broker_url: str = "http://broker:8060"

def load_agent_config(config_path: str = "/app/config.yaml") -> AgentConfig:
    """Fails fast if file missing. No env var fallback."""
```

From agents/_base/kubex_harness/standalone.py (after Plan 02):
```python
# StandaloneConfig is DELETED
# StandaloneAgent now accepts AgentConfig
class StandaloneAgent:
    def __init__(self, config: AgentConfig) -> None:
```

Files that reference StandaloneConfig or KUBEX_AGENT_ID (must be updated):
- tests/unit/test_harness_unit.py — imports StandaloneConfig
- tests/unit/test_config_loader.py — tests env var fallback path
- tests/unit/test_orchestrator_loop.py — imports OrchestratorConfig(StandaloneConfig), sets KUBEX_AGENT_ID
- tests/e2e/test_agent_harness.py — sets KUBEX_AGENT_ID
- tests/unit/test_kubex_manager_unit.py — sets KUBEX_AGENT_ID
- tests/e2e/test_base_image_e2e.py — sets KUBEX_AGENT_ID
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conftest fixture and migrate test files</name>
  <files>
    tests/conftest.py,
    tests/unit/test_config_loader.py,
    tests/unit/test_harness_unit.py,
    tests/unit/test_orchestrator_loop.py,
    tests/e2e/test_agent_harness.py
  </files>
  <action>
**Conftest fixture** (per locked decision: "One batch via conftest fixture"):

1. Read `tests/conftest.py` first to understand existing fixtures.
2. Add a session-scoped fixture `default_agent_config` that:
   - Uses `tmp_path_factory.mktemp("agent_config")` to create a temp directory
   - Writes a minimal `config.yaml` to it: `agent:\n  id: test-agent\n  model: gpt-5.2\n  capabilities: []\n  skills: []\n`
   - Returns the path as a string
   - Per locked decision: "Write real file to tmp_path; monkeypatches config path to point there; exercises real file-reading code path"
3. Add an autouse session-scoped fixture `_patch_default_config_path` that monkeypatches `kubex_harness.config_loader.load_agent_config.__defaults__` to use the default_agent_config path. This way any test that calls `load_agent_config()` with no args gets the test config instead of trying to read `/app/config.yaml`. Tests that pass explicit paths are unaffected.

**test_config_loader.py** migration:

4. Read the file first. Find `TestEnvVarFallback` or similar test classes that test the env-var-only path.
5. Delete `test_fallback_to_env_vars_when_no_config` (behavior explicitly removed per locked decision).
6. Convert any test that checks "missing config + env var = works" to assert `ValueError` is raised when config file is missing: `with pytest.raises(ValueError, match="Required config file not found")`.
7. Add new test: `test_missing_agent_id_raises` -- writes config.yaml with no `agent.id` field, asserts ValueError.
8. Update any remaining tests to write config.yaml to tmp_path and pass the path to `load_agent_config()`.

**test_harness_unit.py** migration:

9. Read the file. Remove all `from kubex_harness.standalone import StandaloneConfig` imports.
10. Replace `StandaloneConfig()` usage with `AgentConfig(agent_id="test", model="gpt-5.2")` from config_loader.
11. Update `StandaloneAgent` instantiation to use `AgentConfig` instead of `StandaloneConfig`.
12. Remove any `os.environ["KUBEX_AGENT_ID"]` setup/teardown.

**test_orchestrator_loop.py** migration:

13. Read the file. This is the most complex migration:
    - Remove `sys.path` hack that adds `agents/orchestrator` to path
    - Remove `from orchestrator_loop import OrchestratorConfig, ...` imports
    - The tool functions now live in `skills/orchestration/task-management/tools/`. Update imports to reference the new locations.
    - Remove `OrchestratorConfig(StandaloneConfig)` references -- use `AgentConfig` from config_loader
    - Remove `os.environ["KUBEX_AGENT_ID"]` setup
    - If tests are testing the multi-turn loop that was part of orchestrator_loop.py, and that loop still exists in the harness (e.g., in standalone.py or main.py), update the import path. If the loop was moved to a skill tool, update accordingly.
    - If individual tool handler functions are tested, update imports to `from skills.orchestration.task_management.tools.{tool_name} import {function}` -- but verify the import path works (skills directory may not be on sys.path; may need to add it or use importlib).

**test_agent_harness.py** migration:

14. Read the file. Remove `KUBEX_AGENT_ID` env var usage. Use the conftest fixture's default config instead.

**Other files** (test_kubex_manager_unit.py, test_base_image_e2e.py):

15. Check if these files set `KUBEX_AGENT_ID` for config initialization or just as a test value for other purposes. If they set it only as mock data for the Manager/Gateway tests (not for harness config loading), they may not need changes. Only update if they depend on StandaloneConfig or the env-var-based config path.
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_config_loader.py tests/unit/test_harness_unit.py tests/unit/test_orchestrator_loop.py tests/e2e/test_agent_harness.py -x -q</automated>
  </verify>
  <done>
- No test file imports StandaloneConfig
- No test file sets KUBEX_AGENT_ID for config initialization purposes
- conftest.py has default_agent_config session fixture
- test_config_loader.py tests fail-fast behavior (ValueError on missing file)
- test_orchestrator_loop.py imports from new skill tool locations
- All 4 updated test files pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove xfail markers from Plan 01 tests and run full regression</name>
  <files>
    tests/e2e/test_agent_migration.py,
    tests/unit/test_no_agent_dockerfiles.py,
    tests/e2e/test_hello_world_spawn.py
  </files>
  <action>
1. Read `tests/e2e/test_agent_migration.py`, `tests/unit/test_no_agent_dockerfiles.py`, and `tests/e2e/test_hello_world_spawn.py` (the Plan 01 red tests).
2. Remove all `@pytest.mark.xfail(strict=True, ...)` markers from every test class/method in these files. The tests should now pass green after Plan 02 implementation.
3. Run the full test suite: `python -m pytest tests/ -q`. Expect 856+ tests passing, 0 failures. The E2E Docker tests will either pass (if Docker is available) or skip (if not).
4. If any tests fail:
   - Investigate root cause immediately (per CLAUDE.md zero failing tests policy)
   - Fix production code if test expectation is correct
   - Fix or remove test if it is outdated
   - Do NOT leave any failing tests
5. Run `ruff check` on all Plan 07 changed files (tests + skills + agents + docker-compose.yml).
6. Run `black --check` on all Python files changed in this phase.
7. Verify no XPASS results exist (all xfail markers should be removed, not left as stale markers).
  </action>
  <verify>
    <automated>python -m pytest tests/ -q && ruff check tests/e2e/test_agent_migration.py tests/unit/test_no_agent_dockerfiles.py tests/e2e/test_hello_world_spawn.py tests/conftest.py</automated>
  </verify>
  <done>
- Full test suite passes: 856+ tests, 0 failures
- No xfail markers remain on Plan 01 tests
- No XPASS results
- All changed Python files pass ruff + black
- Zero failing tests policy satisfied
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -q` exits 0 with 856+ tests passing
- `python -c "import ast, sys; [sys.exit(1) for f in ['tests/unit/test_harness_unit.py','tests/unit/test_orchestrator_loop.py'] if 'StandaloneConfig' in open(f).read()]"` exits 0 (no StandaloneConfig references)
- `ruff check tests/ agents/_base/ skills/` passes
- `black --check tests/ agents/_base/` passes
</verification>

<success_criteria>
- Full test suite: 856+ passed, 0 failed
- No test imports StandaloneConfig
- conftest fixture provides default config for all tests
- All Plan 01 xfail markers removed, tests pass green
- Ruff + black clean on all Phase 7 files
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-migration-and-dockerfile-removal/07-03-SUMMARY.md`
</output>
