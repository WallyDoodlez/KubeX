---
phase: 10-hooks-monitoring
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/test_hook_server.py
  - tests/unit/test_gateway_audit.py
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - HOOK-04

must_haves:
  truths:
    - "Test stub files exist and are collected by pytest without errors"
    - "Each requirement has at least one test stub that skips with a clear message"
  artifacts:
    - path: "tests/unit/test_hook_server.py"
      provides: "Test stubs for hook endpoint, validation, handlers, audit write"
      contains: "pytest.skip"
    - path: "tests/unit/test_gateway_audit.py"
      provides: "Test stubs for Gateway audit endpoint"
      contains: "pytest.skip"
  key_links: []
---

<objective>
Create test stub scaffolding for Phase 10 Hooks Monitoring to satisfy Nyquist compliance. All subsequent plans reference these test files for their verify commands.

Purpose: Ensure every plan task has a runnable automated verify command from the start.
Output: Two test stub files with pytest.skip() placeholders for all HOOK-01 through HOOK-04 test cases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-hooks-monitoring/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for all Phase 10 requirements</name>
  <files>tests/unit/test_hook_server.py, tests/unit/test_gateway_audit.py</files>
  <read_first>
    tests/unit/test_cli_runtime.py
  </read_first>
  <action>
Create two test stub files following existing project test patterns (pytest, async where needed).

**File: tests/unit/test_hook_server.py**

Create test stubs with `pytest.skip("Plan 01 implementation")` for:

HOOK-01 (endpoint receives events):
- `TestHookEndpoint::test_post_tool_use_accepted` — POST /hooks with PostToolUse JSON returns 200
- `TestHookEndpoint::test_stop_event_accepted` — POST /hooks with Stop JSON returns 200
- `TestHookEndpoint::test_session_end_accepted` — POST /hooks with SessionEnd JSON returns 200
- `TestHookEndpoint::test_subagent_stop_accepted` — POST /hooks with SubagentStop JSON returns 200
- `TestHookEndpoint::test_unknown_event_type_returns_200` — POST /hooks with unknown hook_event_name returns 200 (not 422)
- `TestHookEndpoint::test_malformed_payload_returns_200` — POST /hooks with invalid JSON structure returns 200

HOOK-02 (security):
- `TestHookSecurity::test_injection_payload_discarded` — POST /hooks with shell injection in tool_name does not cause execution, returns 200

HOOK-03 (lifecycle events):
- `TestHookHandlers::test_stop_calls_post_progress` — Stop event calls CLIRuntime._post_progress with task_id
- `TestHookHandlers::test_stop_no_task_id_skipped` — Stop event with no current task_id does not call _post_progress

HOOK-04 (audit trail write):
- `TestAuditTrail::test_post_tool_use_writes_audit_entry` — PostToolUse event writes to Redis sorted set audit:{task_id}
- `TestAuditTrail::test_audit_key_has_24h_ttl` — audit:{task_id} key has EXPIRE set to 86400
- `TestAuditTrail::test_audit_entry_minimal_fields` — audit entry contains only tool_name, timestamp, success
- `TestAuditTrail::test_no_task_id_discards_event` — PostToolUse with no current task_id does not write to Redis

**File: tests/unit/test_gateway_audit.py**

HOOK-04 (audit trail read):
- `test_audit_endpoint_returns_entries` — GET /tasks/{task_id}/audit returns JSON array of audit entries
- `test_audit_endpoint_empty_task` — GET /tasks/{task_id}/audit for unknown task_id returns empty entries array
- `test_audit_endpoint_redis_unavailable` — GET /tasks/{task_id}/audit returns 503 when Redis is down

Each stub function body is:
```python
pytest.skip("Plan NN implementation")
```

Use `import pytest` at the top. Use `class` grouping as shown above. Mark async tests with `@pytest.mark.asyncio` where the test name suggests async behavior (audit trail tests that mock Redis).
  </action>
  <verify>
    <automated>cd D:/dev/dev/openclaw && python -m pytest tests/unit/test_hook_server.py tests/unit/test_gateway_audit.py --collect-only -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_hook_server.py contains "class TestHookEndpoint"
    - tests/unit/test_hook_server.py contains "class TestHookSecurity"
    - tests/unit/test_hook_server.py contains "class TestHookHandlers"
    - tests/unit/test_hook_server.py contains "class TestAuditTrail"
    - tests/unit/test_hook_server.py contains "def test_post_tool_use_accepted"
    - tests/unit/test_hook_server.py contains "def test_unknown_event_type_returns_200"
    - tests/unit/test_hook_server.py contains "def test_stop_calls_post_progress"
    - tests/unit/test_hook_server.py contains "def test_post_tool_use_writes_audit_entry"
    - tests/unit/test_gateway_audit.py contains "def test_audit_endpoint_returns_entries"
    - tests/unit/test_gateway_audit.py contains "def test_audit_endpoint_redis_unavailable"
    - pytest --collect-only reports 16 tests collected with 0 errors
  </acceptance_criteria>
  <done>Both test stub files exist, pytest collects all 16 test stubs without import errors, all skip with clear messages referencing which plan implements them.</done>
</task>

</tasks>

<verification>
python -m pytest tests/unit/test_hook_server.py tests/unit/test_gateway_audit.py --collect-only -q
</verification>

<success_criteria>
- 16 test stubs collected by pytest
- 0 import errors
- All stubs use pytest.skip() with plan reference
</success_criteria>

<output>
After completion, create `.planning/phases/10-hooks-monitoring/10-00-SUMMARY.md`
</output>
