---
phase: 07-agent-migration-and-dockerfile-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/test_agent_migration.py
  - tests/unit/test_no_agent_dockerfiles.py
  - tests/e2e/test_hello_world_spawn.py
autonomous: true
requirements: [MIGR-01, MIGR-02, MIGR-03, MIGR-04, MIGR-05]

must_haves:
  truths:
    - "E2E tests exist that assert each agent boots from kubexclaw-base"
    - "A unit test asserts no per-agent Dockerfiles exist in agents/ subdirectories"
    - "An E2E test asserts a new agent can be spawned with only a skill file and config.yaml"
    - "All new tests fail (red) because the migration has not been implemented yet"
  artifacts:
    - path: "tests/e2e/test_agent_migration.py"
      provides: "E2E boot assertions for orchestrator, instagram-scraper, knowledge, reviewer"
      contains: "TestOrchestratorBootsFromBase"
    - path: "tests/unit/test_no_agent_dockerfiles.py"
      provides: "Filesystem assertion that no Dockerfile exists in agent directories"
      contains: "test_no_agent_dockerfiles"
    - path: "tests/e2e/test_hello_world_spawn.py"
      provides: "E2E test for stem cell promise: spawn agent from skill + config only"
      contains: "test_hello_world_agent_boots"
  key_links:
    - from: "tests/e2e/test_agent_migration.py"
      to: "agents/_base/kubex_harness/config_loader.py"
      via: "config.yaml loading inside container"
      pattern: "load_agent_config"
---

<objective>
Write failing (red) tests for all Phase 7 migration requirements: each agent boots from kubexclaw-base, no per-agent Dockerfiles remain, and a new agent can be spawned with only skill + config.

Purpose: Establish the test-first contract per implement-feature workflow (CLAUDE.md). All tests must fail now and pass after Plan 02+03 complete.
Output: Three test files covering MIGR-01 through MIGR-05 plus the hello-world stem cell E2E.
</objective>

<execution_context>
@C:/Users/naaht/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/naaht/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-agent-migration-and-dockerfile-removal/07-CONTEXT.md
@.planning/phases/07-agent-migration-and-dockerfile-removal/07-RESEARCH.md

<interfaces>
<!-- Key patterns from existing E2E tests that the executor should reuse -->

From tests/e2e/test_base_image_e2e.py (existing pattern):
```python
# Docker availability check + skipif
_DOCKER_AVAILABLE = False
try:
    import docker
    docker.from_env().ping()
    _DOCKER_AVAILABLE = True
except Exception:
    pass

@pytest.mark.e2e
@pytest.mark.skipif(not _DOCKER_AVAILABLE, reason="Docker not available")
class TestBaseImageE2E:
    ...
```

From agents/_base/kubex_harness/config_loader.py:
```python
class AgentConfig(BaseModel):
    agent_id: str = ""
    model: str = "gpt-5.2"
    skills: list[str] = Field(default_factory=list)
    capabilities: list[str] = Field(default_factory=list)
    harness_mode: str = "standalone"
    gateway_url: str = "http://gateway:8080"
    broker_url: str = "http://broker:8060"

def load_agent_config(config_path: str = "/app/config.yaml") -> AgentConfig:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write red E2E tests for agent migration and Dockerfile removal</name>
  <files>tests/e2e/test_agent_migration.py, tests/unit/test_no_agent_dockerfiles.py</files>
  <action>
Create `tests/e2e/test_agent_migration.py` with xfail(strict=True) E2E tests for MIGR-01 through MIGR-04. Use the same Docker availability skipif pattern from `tests/e2e/test_base_image_e2e.py`. Tests:

1. **TestOrchestratorBootsFromBase** (MIGR-01): Write a config.yaml to tmp_path with `agent.id: orchestrator`, `agent.model: gpt-5.2`, `agent.skills: ["task-management"]`, `agent.capabilities: ["task_orchestration", "task_management"]`. Run container from `kubexclaw-base:latest` with config bind-mounted to `/app/config.yaml:ro`. Execute `python -c "from kubex_harness.config_loader import load_agent_config; c = load_agent_config(); print(c.agent_id)"`. Assert exit_code == 0 and "orchestrator" in logs. Mark `@pytest.mark.xfail(strict=True, reason="MIGR-01: orchestrator not yet migrated")`.

2. **TestInstagramScraperBootsFromBase** (MIGR-02): Same pattern with `agent.id: instagram-scraper`, `agent.skills: ["web-scraping"]`, `agent.capabilities: ["scrape_instagram", "extract_metrics"]`. Mark xfail MIGR-02.

3. **TestKnowledgeAgentBootsFromBase** (MIGR-03): Same pattern with `agent.id: knowledge`, `agent.skills: ["recall"]`, `agent.capabilities: ["knowledge_management", "knowledge_query", "knowledge_storage"]`. Mark xfail MIGR-03.

4. **TestReviewerBootsFromBase**: Same pattern with `agent.id: reviewer`, `agent.model: o3-mini`, `agent.skills: ["review"]`, `agent.capabilities: ["security_review"]`. Mark xfail (reviewer migration part of Phase 7 per locked decision).

5. **TestNoAgentDockerfiles** (MIGR-04): In `tests/unit/test_no_agent_dockerfiles.py`, write a unit test that scans `agents/` subdirectories (excluding `_base/`) and asserts no `Dockerfile` exists in any of them. Mark xfail MIGR-04. This is a fast filesystem assertion, not Docker-dependent.

For E2E tests, use `docker.from_env()` and `container.run()` with `remove=True`, `stderr=True`. Use `yaml.dump()` to write config files. Use `importorskip("docker")` at module level per Phase 5 decision.

For the MIGR-05 red test: Add a test class `TestFullSuiteRegression` in `test_agent_migration.py` that is marked `@pytest.mark.xfail(strict=True, reason="MIGR-05: StandaloneConfig not yet removed")` and imports `load_agent_config`, writes a minimal config.yaml to tmp_path, monkeypatches the default config path, and asserts that calling `load_agent_config()` with no args raises ValueError when no file at `/app/config.yaml` (this tests the fail-fast behavior that StandaloneConfig removal will enable — currently it silently falls back to env vars).
  </action>
  <verify>
    <automated>python -m pytest tests/e2e/test_agent_migration.py tests/unit/test_no_agent_dockerfiles.py --co -q</automated>
  </verify>
  <done>All test classes are collected by pytest. All tests are marked xfail(strict=True). Running tests produces xfail results (not errors). Test count: at least 6 tests (4 agent boots + 1 no-dockerfiles + 1 fail-fast config).</done>
</task>

<task type="auto">
  <name>Task 2: Write red E2E test for hello-world stem cell spawn</name>
  <files>tests/e2e/test_hello_world_spawn.py</files>
  <action>
Create `tests/e2e/test_hello_world_spawn.py` with an xfail(strict=True) E2E test proving the stem cell promise. Use Docker skipif pattern.

1. **TestHelloWorldSpawn**: Write a minimal skill directory in tmp_path: `hello-world/SKILL.md` (content: "You are a hello-world test agent. Respond with 'Hello from KubexClaw!'"), `hello-world/manifest.yaml` (name: hello-world, version: 0.1.0, category: examples, capabilities: [hello], tools: [], dependencies: {pip: [], system: []}, egress_domains: []). Write a config.yaml: `agent.id: hello-world`, `agent.model: gpt-5.2`, `agent.skills: ["hello-world"]`, `agent.capabilities: ["hello"]`.

2. Run container from `kubexclaw-base:latest` with both the config and skill dir bind-mounted (`/app/config.yaml:ro` and `/app/skills/hello-world:ro`). Execute `python -c "from kubex_harness.config_loader import load_agent_config; c = load_agent_config(); print(c.agent_id, c.skills)"`. Assert exit_code == 0 and "hello-world" in logs and "['hello-world']" in logs.

3. Mark `@pytest.mark.xfail(strict=True, reason="hello-world skill dir and template not yet created")`.

This test proves that an operator can spawn a new agent role by adding a skill file and config.yaml with no Docker build step.
  </action>
  <verify>
    <automated>python -m pytest tests/e2e/test_hello_world_spawn.py --co -q</automated>
  </verify>
  <done>Test is collected by pytest. Test is marked xfail(strict=True). Running the test produces xfail (not error).</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/e2e/test_agent_migration.py tests/unit/test_no_agent_dockerfiles.py tests/e2e/test_hello_world_spawn.py --co -q` collects all tests
- `python -m pytest tests/e2e/test_agent_migration.py tests/unit/test_no_agent_dockerfiles.py tests/e2e/test_hello_world_spawn.py -x -q` shows all xfail (no errors, no unexpected passes)
- Existing test suite still passes: `python -m pytest tests/unit/ tests/integration/ -x -q`
</verification>

<success_criteria>
- At least 7 test functions exist across the 3 new test files
- All new tests are xfail(strict=True) and produce xfail results when run
- No existing tests broken by adding these files
- Tests cover all 5 MIGR requirements plus reviewer and hello-world
</success_criteria>

<output>
After completion, create `.planning/phases/07-agent-migration-and-dockerfile-removal/07-01-SUMMARY.md`
</output>
