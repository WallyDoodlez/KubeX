---
phase: 06-manager-spawn-policy-gates
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - services/kubex-manager/kubex_manager/config_builder.py
  - services/kubex-manager/kubex_manager/redis_store.py
  - services/kubex-manager/kubex_manager/skill_resolver.py
  - services/kubex-manager/kubex_manager/lifecycle.py
  - services/kubex-manager/kubex_manager/main.py
  - services/gateway/gateway/policy.py
  - services/gateway/gateway/main.py
  - libs/kubex-common/kubex_common/schemas/actions.py
  - policies/global.yaml
  - docker-compose.yml
autonomous: true
requirements: [KMGR-01, KMGR-02, KMGR-03, KMGR-04, KMGR-05, PSEC-01, PSEC-02, PSEC-03]

must_haves:
  truths:
    - "SkillResolver.resolve_from_config(agent_config) returns a ComposedSkillSet using skills from agent config"
    - "ConfigBuilder.build() produces a valid config.yaml with model from agent config, capabilities/tools from skills, overrides applied last"
    - "create_kubex() runs an 8-step atomic spawn pipeline with full rollback on failure"
    - "POST /policy/skill-check returns ALLOW for allowlisted skills, ESCALATE for unknown or missing policy"
    - "KubexRecord persists to Redis on every state change and recovers on Manager restart"
    - "Network name resolved by Docker label lookup on every container create, not from env var"
    - "Boot-time deps install without policy gate; runtime install_dependency goes through approve/deny/ESCALATE"
    - "Manager API has respawn, install-dep, config inspect, and configs list endpoints"
  artifacts:
    - path: "services/kubex-manager/kubex_manager/config_builder.py"
      provides: "ConfigBuilder class with build() method"
      exports: ["ConfigBuilder", "ConfigBuildError"]
    - path: "services/kubex-manager/kubex_manager/redis_store.py"
      provides: "KubexRecordStore for Redis write-through persistence"
      exports: ["KubexRecordStore"]
    - path: "services/kubex-manager/kubex_manager/lifecycle.py"
      provides: "Extended KubexLifecycle with spawn pipeline, network label lookup, install_dep"
      contains: "_resolve_internal_network"
    - path: "services/gateway/gateway/main.py"
      provides: "POST /policy/skill-check endpoint"
      contains: "skill-check"
    - path: "services/gateway/gateway/policy.py"
      provides: "AgentPolicy with allowed_skills field"
      contains: "allowed_skills"
    - path: "libs/kubex-common/kubex_common/schemas/actions.py"
      provides: "INSTALL_DEPENDENCY enum value"
      contains: "INSTALL_DEPENDENCY"
    - path: "policies/global.yaml"
      provides: "package_blocklist section"
      contains: "package_blocklist"
    - path: "docker-compose.yml"
      provides: "kubex.network=internal label on network"
      contains: "kubex.network"
  key_links:
    - from: "services/kubex-manager/kubex_manager/lifecycle.py"
      to: "services/kubex-manager/kubex_manager/config_builder.py"
      via: "spawn pipeline step 3"
      pattern: "ConfigBuilder.*build"
    - from: "services/kubex-manager/kubex_manager/lifecycle.py"
      to: "services/kubex-manager/kubex_manager/redis_store.py"
      via: "spawn pipeline step 7"
      pattern: "redis_store.*save"
    - from: "services/kubex-manager/kubex_manager/lifecycle.py"
      to: "services/gateway/gateway/main.py"
      via: "spawn pipeline step 4 — POST /policy/skill-check"
      pattern: "policy/skill-check"
    - from: "services/gateway/gateway/main.py"
      to: "services/gateway/gateway/policy.py"
      via: "skill-check reads allowed_skills from AgentPolicy"
      pattern: "allowed_skills"
    - from: "services/kubex-manager/kubex_manager/lifecycle.py"
      to: "docker SDK"
      via: "_resolve_internal_network label lookup"
      pattern: "networks.list.*filters.*label"
---

<objective>
Implement all 8 Phase 6 requirements to make the red tests from Plan 01 pass (green). Create ConfigBuilder, KubexRecordStore, extend SkillResolver with agent-config input, wire the atomic spawn pipeline with rollback, add POST /policy/skill-check to Gateway, add install_dependency action type with blocklist/soft-limit, implement Redis state persistence, dynamic network label lookup, and new Manager API endpoints.

Purpose: Deliver the complete Manager spawn logic and policy gate infrastructure as independently testable Python units.
Output: 2 new modules (config_builder.py, redis_store.py), 8 extended modules, all Plan 01 red tests turning green.
</objective>

<execution_context>
@C:/Users/naaht/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/naaht/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-manager-spawn-policy-gates/06-CONTEXT.md
@.planning/phases/06-manager-spawn-policy-gates/06-RESEARCH.md
@.planning/phases/06-manager-spawn-policy-gates/06-01-SUMMARY.md

<interfaces>
<!-- Key types the executor builds against. Extracted from codebase. -->

From services/kubex-manager/kubex_manager/skill_resolver.py:
```python
class SkillResolutionError(Exception): ...

@dataclass
class ComposedSkillSet:
    capabilities: list[str]
    pip_deps: list[str]
    system_deps: list[str]
    egress_domains: list[str]
    tools: dict[str, SkillTool]
    ordered_skill_names: list[str]
    version_conflicts: list[str]

class SkillResolver:
    def resolve(self, skill_names: list[str], skill_dir: Path) -> ComposedSkillSet: ...
    # NEW: resolve_from_config(agent_config: dict, skill_dir: Path) -> ComposedSkillSet
```

From services/kubex-manager/kubex_manager/lifecycle.py:
```python
@dataclass
class CreateKubexRequest:
    config: dict[str, Any]
    resource_limits: dict[str, Any] = field(default_factory=dict)
    image: str = "kubexclaw-base:latest"
    gateway_url: str = GATEWAY_URL_DEFAULT
    registry_url: str = REGISTRY_URL_DEFAULT
    skill_mounts: list[str] = field(default_factory=list)

@dataclass
class KubexRecord:
    kubex_id: str
    agent_id: str
    boundary: str
    container_id: str
    status: str
    config: dict[str, Any]
    image: str
    # NEW fields: skills, config_path, runtime_deps, composed_capabilities

class KubexLifecycle:
    def __init__(self, gateway_url, registry_url, redis_client=None): ...
    def create_kubex(self, request: CreateKubexRequest) -> KubexRecord: ...
    # NEW: _resolve_internal_network(docker_client) -> str
    # NEW: load_from_redis() -> None
```

From services/gateway/gateway/policy.py:
```python
class PolicyDecision(str, Enum):
    ALLOW = "allow"
    DENY = "deny"
    ESCALATE = "escalate"

@dataclass
class PolicyResult:
    decision: PolicyDecision
    reason: str
    rule_matched: str | None = None
    agent_id: str | None = None

@dataclass
class AgentPolicy:
    agent_id: str
    allowed_actions: list[str] = field(default_factory=list)
    blocked_actions: list[str] = field(default_factory=list)
    # ... existing fields
    # NEW: allowed_skills: list[str] = field(default_factory=list)
```

From libs/kubex-common/kubex_common/schemas/actions.py:
```python
class ActionType(str, enum.Enum):
    # ... existing 23 values
    # NEW: INSTALL_DEPENDENCY = "install_dependency"
```

Spawn pipeline order (locked decision):
1. Validate agent config
2. SkillResolver.resolve_from_config()
3. ConfigBuilder.build()
4. POST /policy/skill-check
5. Write config.yaml to disk (done by step 3)
6. Create Docker container
7. Persist to Redis
8. Return
Full rollback on any failure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConfigBuilder, KubexRecordStore, extend SkillResolver and schemas</name>
  <files>services/kubex-manager/kubex_manager/config_builder.py, services/kubex-manager/kubex_manager/redis_store.py, services/kubex-manager/kubex_manager/skill_resolver.py, libs/kubex-common/kubex_common/schemas/actions.py, services/gateway/gateway/policy.py, policies/global.yaml</files>
  <action>
    **Create `config_builder.py`** (new file in services/kubex-manager/kubex_manager/):
    - `ConfigBuildError(Exception)` — raised on conflict, missing tool, or validation failure
    - `ConfigBuilder` class with `build(agent_config: dict, composed: ComposedSkillSet, skill_dir: Path, output_dir: Path) -> Path` method
    - Build merges: agent identity (id, boundary, model, budget, policy, providers) from agent_config; capabilities union from composed; tools namespaced as "{skill}.{tool}" from composed.tools; dependencies from composed.pip_deps + composed.system_deps; egress_domains union from composed (with conflict validation); skills list from composed.ordered_skill_names
    - Model comes ONLY from agent_config, never from skills (locked decision)
    - Resource limits come from agent_config (locked decision)
    - Tool existence validation: for each tool in composed.tools, check that `skill_dir / skill_name / "tools" / f"{tool_name}.py"` exists; raise ConfigBuildError listing all missing tools
    - Agent config overrides: if agent_config has "overrides" key, apply it last to modify skill contributions (deep merge)
    - Output: write config dict as YAML to `output_dir / f"{agent_id}.yaml"`; return the Path
    - Conflicts fail the spawn: ConfigBuildError lists all conflicts

    **Create `redis_store.py`** (new file in services/kubex-manager/kubex_manager/):
    - `KubexRecordStore` class with `__init__(self, redis_client)`, `save(record: KubexRecord)`, `delete(kubex_id: str)`, `load_all() -> dict[str, KubexRecord]`
    - Redis key pattern: `kubex:record:{kubex_id}`
    - Uses `json.dumps(record.to_dict())` / `json.loads()` + `KubexRecord.from_dict()`
    - No TTL — records persist until explicit delete (locked decision)
    - `load_all()` uses `redis.scan_iter("kubex:record:*")` pattern

    **Extend `skill_resolver.py`:**
    - Add `resolve_from_config(self, agent_config: dict, skill_dir: Path) -> ComposedSkillSet` method
    - Extracts skill names from `agent_config["skills"]`; raises SkillResolutionError if key missing
    - If `agent_config` has "overrides" key, applies overrides to the composed result
    - Calls existing `self.resolve(skill_names, skill_dir)` internally

    **Extend `KubexRecord`** in lifecycle.py:
    - Add fields: `skills: list[str] = field(default_factory=list)`, `config_path: str | None = None`, `runtime_deps: list[str] = field(default_factory=list)`, `composed_capabilities: list[str] = field(default_factory=list)`
    - Add `to_dict() -> dict` and `@classmethod from_dict(cls, data: dict) -> KubexRecord` methods
    - Ensure all fields are JSON-serializable (no datetime, no Path objects in dict output)

    **Extend `ActionType` enum** in actions.py:
    - Add `INSTALL_DEPENDENCY = "install_dependency"` value

    **Extend `AgentPolicy`** in gateway/policy.py:
    - Add `allowed_skills: list[str] = field(default_factory=list)` field
    - Update `PolicyLoader._load_agent_policy()` to read `allowed_skills` from agent policy YAML under `agent_policy:` → `allowed_skills:`

    **Extend `GlobalPolicy`** in gateway/policy.py:
    - Add `package_blocklist: dict[str, list[str]] = field(default_factory=dict)` field (keyed by package type: "pip", "cli")
    - Update `PolicyLoader._load_global()` to read `package_blocklist` from global.yaml

    **Extend `policies/global.yaml`:**
    - Add `package_blocklist:` section with `pip:` list containing seed entries: paramiko, pwntools, scapy, cryptography
    - Add `cli:` list (empty initially — Claude's discretion for seed entries)
    - Add `runtime_install_soft_limit: 10` (Claude's discretion for default value)
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_config_builder.py tests/unit/test_skill_resolver.py tests/unit/test_gateway_policy.py -x --tb=short -q 2>&1 | tail -20</automated>
  </verify>
  <done>ConfigBuilder produces valid config.yaml from agent_config + ComposedSkillSet. KubexRecordStore saves/loads KubexRecords to Redis. SkillResolver accepts agent_config dict. ActionType.INSTALL_DEPENDENCY exists. AgentPolicy has allowed_skills. GlobalPolicy has package_blocklist. All related Plan 01 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire spawn pipeline, Gateway skill-check endpoint, network label lookup, install-dep flow, and Manager API endpoints</name>
  <files>services/kubex-manager/kubex_manager/lifecycle.py, services/kubex-manager/kubex_manager/main.py, services/gateway/gateway/main.py, docker-compose.yml</files>
  <action>
    **Rewrite `create_kubex()` as atomic spawn pipeline** in lifecycle.py:
    - Pipeline order (locked decision): (1) Validate agent config → (2) SkillResolver.resolve_from_config() → (3) ConfigBuilder.build() → (4) POST /policy/skill-check via httpx to gateway_url → (5) config.yaml already written by step 3 → (6) Docker container create (with config.yaml bind-mounted at /app/config.yaml + skill bind mounts) → (7) Persist KubexRecord to Redis via KubexRecordStore → (8) Auto-start container and return record
    - Track rollback state: `config_path: Path | None = None`, `container_id: str | None = None`
    - On ANY exception: remove container (force=True) if container_id set, delete config_path if exists, re-raise
    - Wrap container.remove in its own try/except to prevent rollback failure masking original error
    - The method should be sync (Docker SDK is sync); Redis persistence happens in the async handler after create_kubex returns, following the existing pattern in main.py:144-147 where lifecycle events are published post-create. OR make create_kubex itself handle it if the redis_store uses sync Redis. Choose whichever is cleaner — the key constraint is no partial state.

    **Add `_resolve_internal_network(self, docker_client) -> str`** to KubexLifecycle:
    - `docker_client.networks.list(filters={"label": "kubex.network=internal"})`
    - If empty: raise RuntimeError with clear error message including setup instructions
    - Replace the `network=os.environ.get("KUBEX_DOCKER_NETWORK", NETWORK_INTERNAL)` line in container create with `network=self._resolve_internal_network(docker_client)`
    - Look up on every container create call, not cached (locked decision)

    **Add new Manager API endpoints** in main.py:
    - `POST /kubexes/{kubex_id}/respawn` — kills current container, creates new one using persisted config from Redis. Requires Bearer token auth (same pattern as other /kubexes endpoints).
    - `POST /kubexes/{kubex_id}/install-dep` — accepts `{package: str, type: str}` body. Runs `container.exec_run(["pip", "install", package])` for type="pip". Checks exit code (0=success, non-zero=failure). Records installed package in KubexRecord.runtime_deps and persists to Redis. For type="cli", run appropriate installer. All policy-gated by Gateway before this is called.
    - `GET /kubexes/{kubex_id}/config` — returns the full merged config.yaml content as JSON for debugging/auditing
    - `GET /configs` — lists saved config.yaml files with metadata (agent_id, skills, created_at) from the persistent config directory
    - Enrich existing `GET /kubexes` and `GET /kubexes/{kubex_id}` responses to include: skills list, config_path, runtime_deps, composed_capabilities

    **Add `POST /policy/skill-check` endpoint** in gateway/main.py:
    - Request body: `SkillCheckRequest(agent_id: str, skills: list[str])` Pydantic model
    - Read `agent_policy = gateway.policy_loader.get_agent_policy(body.agent_id)`
    - If agent_policy is None or agent_policy.allowed_skills is empty: return PolicyResult(decision=ESCALATE, reason="No skill allowlist for agent '{agent_id}'", rule_matched="agent.skills.no_policy", agent_id=body.agent_id)
    - For each skill in body.skills: if skill not in agent_policy.allowed_skills, return PolicyResult(decision=ESCALATE, reason="Skill '{skill}' not in allowlist for agent '{agent_id}'", rule_matched="agent.skills.escalate", agent_id=body.agent_id)
    - If all skills on allowlist: return PolicyResult(decision=ALLOW, reason="All skills on allowlist", rule_matched="agent.skills.allow", agent_id=body.agent_id)
    - Response format: JSON matching existing PolicyResult serialization (decision, reason, rule_matched, agent_id)

    **Add install_dependency handling** in gateway/main.py action handler:
    - When action is INSTALL_DEPENDENCY: check package against GlobalPolicy.package_blocklist — if in blocklist, DENY (never ESCALATE, locked decision)
    - Check soft limit: count runtime_deps for the agent (via Manager API or Redis); if exceeds runtime_install_soft_limit, ESCALATE
    - If approved (by policy or reviewer): call `POST /kubexes/{kubex_id}/install-dep` on Manager
    - If reviewer approves: auto-add package to persistent config.yaml for future boots (locked decision)

    **Update `docker-compose.yml`:**
    - Add `labels:` section to the `kubex-internal` network definition: `kubex.network: internal`
    - Add `./configs:/app/configs` volume mount to kubex-manager service for persistent config directory

    **PSEC-01 confirmation:** Verify that the boot-time dep install path (entrypoint.sh reads KUBEX_PIP_DEPS/KUBEX_SYSTEM_DEPS from config and runs pip/apt directly) never calls POST /actions on Gateway. This should already be true from Phase 5 — entrypoint.sh installs directly. If there's any policy call in the boot path, remove it. Add a comment in entrypoint.sh confirming: "# Boot deps from config.yaml are trusted — no policy gate (PSEC-01)".
  </action>
  <verify>
    <automated>python -m pytest tests/unit/test_kubex_manager_unit.py tests/unit/test_gateway_endpoints.py tests/unit/test_gateway_policy.py tests/unit/test_harness_unit.py tests/integration/test_redis_state.py -x --tb=short -q 2>&1 | tail -20</automated>
  </verify>
  <done>All Plan 01 red tests pass (green). Spawn pipeline is atomic with rollback. POST /policy/skill-check returns correct ALLOW/ESCALATE. Network resolved by label. KubexRecords persist to Redis. Manager has respawn, install-dep, config, and configs endpoints. Boot deps install without policy gate. install_dependency goes through policy pipeline with blocklist DENY and soft limit ESCALATE.</done>
</task>

</tasks>

<verification>
Run full unit + integration test suite to confirm all Plan 01 tests are green and no regressions:
```bash
python -m pytest tests/ --tb=short -q
```
All previously passing tests still pass. All Plan 01 red tests now green.
</verification>

<success_criteria>
- All ~34 red tests from Plan 01 now pass (green)
- ConfigBuilder produces correct config.yaml structure per locked decisions
- KubexRecord round-trips through Redis without data loss
- Spawn pipeline rolls back cleanly on any step failure
- POST /policy/skill-check returns ALLOW/ESCALATE correctly
- Dynamic network label lookup replaces hardcoded env var
- INSTALL_DEPENDENCY action type exists and is policy-gated
- Boot deps install without policy gate (PSEC-01 confirmed)
- 4 new Manager API endpoints functional
- docker-compose.yml has network label and configs volume
</success_criteria>

<output>
After completion, create `.planning/phases/06-manager-spawn-policy-gates/06-02-SUMMARY.md`
</output>
