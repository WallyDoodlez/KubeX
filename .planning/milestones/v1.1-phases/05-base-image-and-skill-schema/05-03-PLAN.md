---
phase: 05-base-image-and-skill-schema
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified: []
autonomous: true
requirements:
  - BASE-01
  - BASE-02
  - BASE-03
  - BASE-04
  - SKIL-01
  - SKIL-02
  - SKIL-03
  - SKIL-04
must_haves:
  truths:
    - "Full test suite passes with 827+ tests (no regressions from Phase 5 changes)"
    - "No import errors or AttributeError from SkillManifest schema migration"
    - "Linting (ruff) and formatting (black) pass on all changed files"
    - "Skill validator CLI exits 0 on shipped skill catalog"
  artifacts: []
  key_links: []
---

<objective>
Run the complete regression verification for Phase 5: full pytest suite, linting, formatting, and skill validator CLI. Confirm zero regressions from the base image refactor, SkillManifest rewrite, and new modules.

Purpose: Gate check before Phase 6 can begin. The implement-feature workflow requires a clean full-suite run after implementation.

Output: Confirmed test count (827+ preserved), linting green, no regressions. Phase 5 complete.
</objective>

<execution_context>
@C:/Users/naaht/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/naaht/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-base-image-and-skill-schema/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression suite and code quality checks</name>
  <files></files>
  <action>
Run the complete verification battery:

1. **Full test suite:** `python -m pytest tests/ libs/ services/ -v --tb=short` — capture full output. Count total tests. Must be 827+ (the pre-Phase-5 count plus new tests). Zero failures, zero errors.

2. **Linting:** `python -m ruff check agents/_base/ libs/kubex-common/ services/kubex-manager/` — all changed files must pass ruff. Fix any issues found (auto-fix with `ruff check --fix` if safe).

3. **Formatting:** `python -m black --check agents/_base/ libs/kubex-common/ services/kubex-manager/` — all changed files must pass black. Fix with `black agents/_base/ libs/kubex-common/ services/kubex-manager/` if needed.

4. **Skill validator CLI:** `python -m kubex_manager.skill_validator skills/` — must exit 0 against all shipped skill files (3 migrated skill.yaml + SKILL.md pairs).

5. **Import smoke tests:** Run these one-liners to confirm all new modules are importable:
   - `python -c "from kubex_common.schemas.config import SkillManifest, SkillTool, SkillDependencies, ValidationStamp"`
   - `python -c "from kubex_manager.skill_validator import SkillValidator, ValidationVerdict"`
   - `python -c "from kubex_manager.skill_resolver import SkillResolver, ComposedSkillSet, SkillResolutionError"`
   - `python -c "from kubex_harness.config_loader import load_agent_config"`
   - `python -c "from kubex_harness.standalone import _load_skill_files, StandaloneConfig"` (backward compat)

6. **Schema validation smoke test:** `python -c "from kubex_common.schemas.config import SkillManifest; SkillManifest(name='test', capabilities=['cap1'], tools=[], egress_domains=[])"` — must succeed. Then `python -c "from kubex_common.schemas.config import SkillManifest; SkillManifest(name='test', actions_required=['a'])"` — must raise ValidationError (extra field forbidden).

7. If any test fails:
   - Identify root cause (schema change ripple, import path change, etc.)
   - Fix the issue in the appropriate file
   - Re-run full suite to confirm fix
   - Commit the fix: `fix(05): {description of regression fix}`

8. If all green, report final test count and confirm Phase 5 complete.
  </action>
  <verify>
    <automated>cd D:/dev/dev/openclaw && python -m pytest tests/ libs/ services/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Full suite passes with 827+ tests. Ruff and black clean. Skill validator CLI exits 0. All imports work. Phase 5 regression verification complete.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ libs/ services/ -x -q` exits 0 with 827+ tests
- `python -m ruff check agents/_base/ libs/kubex-common/ services/kubex-manager/` exits 0
- `python -m black --check agents/_base/ libs/kubex-common/ services/kubex-manager/` exits 0
- `python -m kubex_manager.skill_validator skills/` exits 0
</verification>

<success_criteria>
- Total test count >= 827 (no tests lost)
- Zero test failures or errors
- Ruff linting passes on all Phase 5 files
- Black formatting passes on all Phase 5 files
- Skill validator CLI exits 0 on shipped catalog
- All new module imports succeed
- Backward-compat imports (`_load_skill_files`, `StandaloneConfig`) still work
</success_criteria>

<output>
After completion, create `.planning/phases/05-base-image-and-skill-schema/05-03-SUMMARY.md`
</output>
