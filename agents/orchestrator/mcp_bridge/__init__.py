"""mcp_bridge — Python-importable package for the MCP Bridge server.

This package provides Python-importable aliases for the mcp-bridge directory
(which uses a dash, making it non-importable as a Python module).

The actual implementation lives in agents/orchestrator/mcp-bridge/server.py
and agents/orchestrator/mcp-bridge/client/gateway.py.
"""
